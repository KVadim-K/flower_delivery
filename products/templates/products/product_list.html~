{% extends "base.html" %}
{% load static %}

{% block title %}Каталог - FlowerDelivery{% endblock %}

{% block content %}
<h1 class="section-title">Каталог Цветов</h1>
<div class="product-grid">
    {% for product in products %}
        <div class="product-card">
            <a href="{{ product.image.url }}" target="_blank" class="product-image">
                <img src="{{ product.image.url }}" alt="{{ product.name }}" loading="lazy">
            </a>
            <div class="product-info">
                <h2 class="product-name">{{ product.name }}</h2>
                <p class="product-price">{{ product.price }} ₽</p>
            </div>
            <a href="{% url 'orders:create_order' product.id %}" class="btn btn-add-to-cart">
                <i class="fas fa-shopping-cart"></i> Добавить в корзину
            </a>
        </div>
    {% empty %}
        <p>Нет доступных продуктов.</p>
    {% endfor %}
</div>
{% endblock %}
